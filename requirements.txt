# CrewAI + RAG (runs locally via Ollama)
crewai==1.10.1
crewai-tools==1.10.1
# Required for CrewAI's LLM() to recognize non-native providers like "ollama/..."
litellm==1.82.3
# Required for RagTool's ollama embedding provider config to validate
ollama==0.6.1

# UI
streamlit==1.55.0

# Speech-to-text (transcription)
openai-whisper==20250625

# Environment variables
python-dotenv==1.1.1

# Verified HTTPS downloads for Whisper model files
certifi>=2024.0.0

# JIRA integration
atlassian-python-api==4.0.7

# Speaker diarization (optional — requires HF_TOKEN)
# pyannote.audio>=3.1.0

# Tests
pytest==9.1.1
